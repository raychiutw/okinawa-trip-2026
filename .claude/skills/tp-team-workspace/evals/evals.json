{
  "skill_name": "tp-team",
  "evals": [
    {
      "id": 1,
      "prompt": "daily-check.js 的表名寫錯，改一行 requests → trip_requests",
      "expected_output": "路由到 MICRO，輸出 MICRO Gate（含 Branch 步驟），流程：branch → build → verify → ship",
      "files": []
    },
    {
      "id": 2,
      "prompt": "新增 GET /api/trips/:id/weather endpoint，回傳該行程日期的天氣資料，要寫 integration tests",
      "expected_output": "路由到 STANDARD，輸出 STANDARD Gate，流程包含 TDD、/simplify、/canary 建議",
      "files": []
    },
    {
      "id": 3,
      "prompt": "把 cron 輪詢改成事件驅動 + SSE 即時推送，需要新 API endpoint、新 hook、migration、launchd job",
      "expected_output": "路由到 MAJOR（多個強制升級條件觸發），/office-hours 標為強烈建議，含實作順序指引和 multi-session 交接",
      "files": []
    }
  ]
}
